import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontWeight: "900",
    flexDirection: "column",
    textAlign: "center",
    fontSize: 32,
    color: "#fff",
    flexShrink: 1,
  },
  subtitle: {
    fontWeight: "700",
    fontSize: 24,
    color: "#fff",
    flexShrink: 1,
    textAlign: "center",
  },
  textButton: {
    fontSize: 16,
    fontWeight: "800",
    textAlign: "center",
  },
});
