import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontWeight: 900,
    margin: -4,
    flexDirection: "column",
    textAlign: "center",
    fontSize: 40,
    color: "#fff",
    flexShrink: 1,
  },
  textButton: {
    fontSize: 16,
    fontWeight: "800",
    textAlign: "center",
  },
});
